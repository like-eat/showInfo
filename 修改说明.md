# 网站修改说明

改完任意内容后，在 `WEB` 目录执行三行命令即可上线：
```bash
git add .
git commit -m "更新内容"
git push
```
推送后自动构建部署，几分钟后刷新 https://0154jkl.github.io/Fiona/ 就是最新的。

---

## 结构说明（现在很简单）

每个项目的**全部内容**（卡片文字 + 详情文字 + 图片文件夹）都集中在自己的一个文件里，放在：

`src/projects/`
- `xinliu.js` —— 搜狐心流Agent
- `newuser.js` —— 爱学APP新手专区活动
- `ligong.js` —— **理工送外卖APP**（以后要替换的就是它）
- `referral.js` —— 爱学转介绍活动
- `website.js` —— 爱学Web官网
- `_template.js` —— 空白模板（没接入，专门用来复制替换用）

图片仍放在项目根目录各自的文件夹：`心流/`、`3新用户/`、`5理工送/`、`2转介绍裂变/`、`5官网/`。

---

## 一、改某个项目的文字

打开对应项目文件（例如理工送就是 `src/projects/ligong.js`），里面就几行：

```js
tag:   { cn: 'APP设计', en: 'UX Project' },        // 卡片小标签
title: { cn: '理工送外卖APP', en: 'Education App' }, // 卡片标题
coverFit: 'object-contain',                        // 封面填充：object-contain 不裁剪 / null 裁剪铺满
detail: {
  cn: '中文正文……',      // 详情页正文（中）
  en: 'English text……',  // 详情页正文（英）
  role: '交互设计',        // 角色（中）
  roleEn: 'UX Design',    // 角色（英）
  year: '2025',           // 年份
},
```
改哪个改哪行，中英各一处，改完保存即可。

## 二、换某个项目的图片

图片在该项目对应的文件夹（理工送 = `5理工送/`）：
- **封面**：文件名以 `Slide` 开头的那张。换封面就替换这张（名字保持 Slide 开头）。
- **详情页长图**：其余图片，按文件名**结尾数字**从小到大排。
  - 替换：覆盖同名文件。
  - 新增：放张新图，编号接着排（如 `9.png`）。
  - 删除：删掉对应文件。
- 项目文件里那两行 `import.meta.glob('../../5理工送/...')` 不用动，图会自动读取。

## 三、调整「项目目录」的顺序 / 增删项目

只改一个地方：`src/data.js` 里这一行的顺序 ——
```js
const PROJECTS = [xinliu, newuser, ligong, referral, website]
```
调换顺序、删掉某项、加新项目都改这一行，封面/文字/详情会一起跟着走，不会错位。

---

## 四、★ 以后整体替换「理工送」为新项目（最省事的方式）

1. 在项目根目录新建一个文件夹放新项目的图（如 `新项目/`），
   封面图命名以 `Slide` 开头，其余长图用结尾数字排序（`1.png`、`2.png`…）。
2. 打开 `src/projects/_template.js`，把里面的**全部内容复制**，
   粘贴覆盖到 `src/projects/ligong.js`。
3. 在 `ligong.js` 里把 4 处 `新项目文件夹` 改成第 1 步的文件夹名，
   再填好 `tag / title / detail` 的文字。
4. `git add . && git commit -m "替换理工送项目" && git push`，等几分钟就上线。

> 这样每次换项目只动 `ligong.js` 一个文件 + 一个图片文件夹，不用再到处找。
